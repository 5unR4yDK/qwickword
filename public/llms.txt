# Qwickword

> Qwickword is a free video calling tool. You pick a time limit
> (1 to 30 minutes), get a shareable link, and the call ends automatically
> the moment the timer hits zero. There's no way to extend it.

## What it does

- Creates a single-use video call link with a hard time limit set by the
  creator.
- Guests join in the browser without an account, download, or installation.
- The countdown starts once a second person joins the call (or the creator
  manually starts it), not the moment the link is created, so the link can
  sit unopened without wasting the call's own time.
- When the timer reaches zero, the call ends for everyone. There is no
  extend button and no way to rejoin the same link afterward.

## Who it's for

People who want a meeting, catch-up, or quick video chat with a pre-agreed hard
stop. Most video call tools are built to keep people on the call; this one is
built to end it.

## Persistent rooms

- A room has a stable `/r/` link, a name, and a default call length so people
  who speak regularly can return to the same place.
- A room is not a permanent call. Each call created inside it gets a fresh,
  separate timed link and the same hard ending.
- Optional sign-in can remember people and recent activity. It is not required
  for a guest to join a call.

## Links

- A created call is a clean, memorable two-word link (e.g.
  https://qwickword.com/quiet-otter). There are no tracking parameters and
  nothing to configure after creation.

## Site

- Homepage (create a link): https://qwickword.com
- How the shared hard stop works, including practical limits:
  https://qwickword.com/how-qwickword-works
- What Qwickword is, in plain terms: https://qwickword.com/about
- Manifesto (the case for short calls): https://qwickword.com/manifesto
