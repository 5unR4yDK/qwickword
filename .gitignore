# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules
node_modules/

# Next.js build output
.next/
out/
build/

# Misc
.DS_Store
*.log
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
/coverage

# TypeScript (Next.js regenerates this on dev/build; don't commit it)
*.tsbuildinfo
next-env.d.ts

# Leftover, harmless artifacts from scaffolding on 2026-07-13 — this workspace mount does not
# allow deleting files once written (rename is allowed, unlink is not), so these were renamed
# out of the way instead. Safe to ignore; safe for Andreas to delete by hand from Windows.
.trash-broken-*
.leftover-symlink-ignore-me
