# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules
node_modules/

# Next.js build output
.next/
out/
build/

# Misc
.DS_Store
*.log
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
/coverage

# TypeScript (Next.js regenerates this on dev/build; don't commit it)
*.tsbuildinfo
next-env.d.ts
.env*
# ...except the checked-in template (placeholder values only, no secrets)
!.env.example

# Vercel CLI project link (contains org/project IDs, not secrets, but local-only)
.vercel

# Internal build/planning docs (2026-07-25) — local-only from here on, not
# part of the public repo. Still live in this folder on disk; the nightly
# monitoring task reads/writes them directly, no git involved for these.
AGENTS.md
ANALYTICS_LOG.md
ASKS.md
BUILDER_CREDIBILITY_CHECKLIST.md
BUILD_PLAN.md
CALL_UI_REBUILD_SPEC.md
CLAUDE.md
MONITORING.md
PROMOTION_QUEUE.md
REVENUE.md
ROADMAP.md
SLOGANS.md
SOMEDAY_IDEAS.md
STATUS.md
